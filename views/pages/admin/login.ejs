
    <h1 class="text-center">Formulaire de connection admin</h1>

    <% if(query.error && query.error !== ''){ %> <%= query.error %> <% }%>

    <form action="/admin/login" method="post" class="d-flex flex-column col-10 col-xxl-3 m-auto bg-primary p-4 rounded-3 gap-3">
      <input name="name" type="text" placeholder="Nom" class="rounded border-0 bg-light py-2"/>
      <input name="password" type="password" placeholder="Mot de passe" class="rounded border-0 bg-light py-2" />
      <input type="submit" />
    </form>

